---
interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<div class:list={["bg-surface border border-line rounded-md px-4 py-3 flex flex-col gap-1.5 hover:bg-surface-hover transition-colors", className]}>
  <slot />
</div>
